# 开发委派约定

> 更新：2026-08-13

当前产品经理/架构师 Agent 保留产品决策、集成、部署和最终验收责任。子任务仅用于边界明确、可独立验证的代码或审查切片。

当前用户指定：所有子任务统一使用 Luna worker，并使用 `max` 推理；不要再调用 Pi、OpenCode、Claude Code、Gemini 或其它本地 CLI Agent。每个子任务必须声明文件所有权、不得回滚共享工作区、给出测试证据和剩余阻断。

历史 CLI/provider/model 探索不再是当前项目实施要求；需要追溯时使用 Git 历史。
